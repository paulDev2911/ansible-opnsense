---
# ansible-galaxy collection install -r requirements.yml

collections:
  - name: ansibleguy.opnsense
    version: ">=0.17.0"
  - name: ansible.utils
    version: ">=2.10.0"
  - name: community.hashi_vault
    version: ">=6.0.0"  # Optional – für OpenBao/Vault Secret Injection
